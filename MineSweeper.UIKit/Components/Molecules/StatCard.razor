@* Molecule: StatCard - Display a statistic with label and value *@

<div class="stat-card">
    <div class="flex justify-between items-center">
        <span class="@GetLabelClasses()">@Label</span>
        <span class="@GetValueClasses()">@Value</span>
    </div>
    @if (!string.IsNullOrEmpty(SubValue))
    {
        <div class="mt-1 text-xs text-gray-400">@SubValue</div>
    }
</div>