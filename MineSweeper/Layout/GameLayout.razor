@inherits LayoutComponentBase

<div class="min-h-screen bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950 relative z-10">
    <nav class="bg-slate-900/80 backdrop-blur-md shadow-xl border-b border-blue-500/20 relative z-20">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center gap-8">
                    <a href="" class="flex items-center gap-2 text-white hover:text-blue-300 transition-colors group">
                        <span class="text-2xl transform group-hover:scale-110 transition-transform">💣</span>
                        <span class="font-bold text-xl bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">MineSweeper</span>
                    </a>
                    
                    <div class="hidden md:flex items-center gap-6">
                        <NavLink href="" Match="NavLinkMatch.All" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all">
                            <span class="text-lg">🎮</span>
                            <span>Play</span>
                        </NavLink>
                        <NavLink href="stats" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all">
                            <span class="text-lg">📊</span>
                            <span>Statistics</span>
                        </NavLink>
                        <NavLink href="achievements" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all">
                            <span class="text-lg">🏆</span>
                            <span>Achievements</span>
                        </NavLink>
                    </div>
                </div>
                
                <div class="md:hidden">
                    <button @onclick="ToggleMobileMenu" class="text-white p-2">
                        <span class="text-2xl">☰</span>
                    </button>
                </div>
            </div>
            
            @if (_showMobileMenu)
            {
                <div class="md:hidden pb-4 space-y-2">
                    <NavLink href="" Match="NavLinkMatch.All" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">🎮</span>
                        <span>Play</span>
                    </NavLink>
                    <NavLink href="stats" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">📊</span>
                        <span>Statistics</span>
                    </NavLink>
                    <NavLink href="achievements" class="flex items-center gap-2 px-3 py-2 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">🏆</span>
                        <span>Achievements</span>
                    </NavLink>
                </div>
            }
        </div>
    </nav>
    
    <main>
        @Body
    </main>
</div>

@code {
    private bool _showMobileMenu = false;
    
    private void ToggleMobileMenu()
    {
        _showMobileMenu = !_showMobileMenu;
    }
    
    private void CloseMobileMenu()
    {
        _showMobileMenu = false;
    }
}