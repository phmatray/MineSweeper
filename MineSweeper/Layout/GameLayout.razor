@inherits LayoutComponentBase

<div class="min-h-screen bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950 relative z-10 flex flex-col">
    <nav class="bg-slate-900/80 backdrop-blur-md shadow-xl border-b border-blue-500/20 relative z-20 sticky top-0">
        <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
            <div class="flex justify-between items-center h-16 lg:h-18">
                <div class="flex items-center gap-6 md:gap-8">
                    <a href="" class="flex items-center gap-2 text-white hover:text-blue-300 transition-all duration-200 group">
                        <span class="text-2xl sm:text-3xl transform group-hover:scale-110 transition-transform duration-200">💣</span>
                        <span class="font-bold text-lg sm:text-xl bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">MineSweeper</span>
                    </a>

                    <Stack Direction="Stack.StackDirection.Row" Align="Stack.StackAlign.Center" Gap="Stack.StackGap.Small" Class="hidden md:flex lg:gap-4">
                        <NavItem Href="" Match="NavLinkMatch.All" Icon="🎮" Label="Play" />
                        <NavItem Href="stats" Icon="📊" Label="Statistics" />
                        <NavItem Href="achievements" Icon="🏆" Label="Achievements" />
                    </Stack>
                </div>

                <div class="md:hidden">
                    <Button OnClick="ToggleMobileMenu"
                            Variant="ButtonVariant.Icon"
                            AriaLabel="Toggle menu">
                        <span class="text-2xl">☰</span>
                    </Button>
                </div>
            </div>

            @if (_showMobileMenu)
            {
                <Stack Direction="Stack.StackDirection.Column" Gap="Stack.StackGap.Small" Class="md:hidden pb-4 animate-in slide-in-from-top-2 duration-200">
                    <NavItem Href="" Match="NavLinkMatch.All" Icon="🎮" Label="Play" IsMobile="true" OnClick="CloseMobileMenu" />
                    <NavItem Href="stats" Icon="📊" Label="Statistics" IsMobile="true" OnClick="CloseMobileMenu" />
                    <NavItem Href="achievements" Icon="🏆" Label="Achievements" IsMobile="true" OnClick="CloseMobileMenu" />
                </Stack>
            }
        </div>
    </nav>

    <main class="flex-1 w-full">
        @Body
    </main>

    <GameFooter />
</div>