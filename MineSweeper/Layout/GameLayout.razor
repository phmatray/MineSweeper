@inherits LayoutComponentBase

<div class="min-h-screen bg-gradient-to-br from-slate-950 via-blue-950 to-slate-950 relative z-10 flex flex-col">
    <nav class="bg-slate-900/80 backdrop-blur-md shadow-xl border-b border-blue-500/20 relative z-20 sticky top-0">
        <div class="mx-auto px-4 sm:px-6 lg:px-8 max-w-7xl">
            <div class="flex justify-between items-center h-16 lg:h-18">
                <div class="flex items-center gap-6 md:gap-8">
                    <a href="" class="flex items-center gap-2 text-white hover:text-blue-300 transition-all duration-200 group">
                        <span class="text-2xl sm:text-3xl transform group-hover:scale-110 transition-transform duration-200">💣</span>
                        <span class="font-bold text-lg sm:text-xl bg-gradient-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent">MineSweeper</span>
                    </a>

                    <div class="hidden md:flex items-center gap-2 lg:gap-4">
                        <NavLink href="" Match="NavLinkMatch.All" class="flex items-center gap-2 px-3 lg:px-4 py-2 rounded-lg text-sm text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500/50">
                            <span class="text-lg">🎮</span>
                            <span class="font-medium">Play</span>
                        </NavLink>
                        <NavLink href="stats" class="flex items-center gap-2 px-3 lg:px-4 py-2 rounded-lg text-sm text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500/50">
                            <span class="text-lg">📊</span>
                            <span class="font-medium">Statistics</span>
                        </NavLink>
                        <NavLink href="achievements" class="flex items-center gap-2 px-3 lg:px-4 py-2 rounded-lg text-sm text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500/50">
                            <span class="text-lg">🏆</span>
                            <span class="font-medium">Achievements</span>
                        </NavLink>
                    </div>
                </div>

                <div class="md:hidden">
                    <Button OnClick="ToggleMobileMenu"
                            Variant="ButtonVariant.Icon"
                            AriaLabel="Toggle menu">
                        <span class="text-2xl">☰</span>
                    </Button>
                </div>
            </div>

            @if (_showMobileMenu)
            {
                <div class="md:hidden pb-4 space-y-2 animate-in slide-in-from-top-2 duration-200">
                    <NavLink href="" Match="NavLinkMatch.All" class="flex items-center gap-2 px-4 py-3 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">🎮</span>
                        <span class="font-medium">Play</span>
                    </NavLink>
                    <NavLink href="stats" class="flex items-center gap-2 px-4 py-3 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">📊</span>
                        <span class="font-medium">Statistics</span>
                    </NavLink>
                    <NavLink href="achievements" class="flex items-center gap-2 px-4 py-3 rounded-lg text-gray-300 hover:text-white hover:bg-blue-500/10 transition-all duration-200 w-full" @onclick="CloseMobileMenu">
                        <span class="text-lg">🏆</span>
                        <span class="font-medium">Achievements</span>
                    </NavLink>
                </div>
            }
        </div>
    </nav>

    <main class="flex-1 w-full">
        @Body
    </main>

    <GameFooter />
</div>

@code {
    private bool _showMobileMenu = false;

    private void ToggleMobileMenu()
    {
        _showMobileMenu = !_showMobileMenu;
    }

    private void CloseMobileMenu()
    {
        _showMobileMenu = false;
    }
}